# vim: set ft=zsh expandtab:

# Report BIOS version and CPU type
biosversion() {
    {
        sudo dmidecode
        sudo lshw -C cpu
    } | command grep --ignore-case --after-context=3 'product:\|vendor:' | sed -e 's/^[[:space:]]*//'
}
