# vim: set ft=zsh expandtab:

clipout() {
    local command=()
    case $OSTYPE in
        darwin*)
            command=(pbpaste) ;;
        *)
            command=(xclip -sel clip -o) ;;
    esac

    if (( $# > 0 )) && [[ ! -d $1 ]]; then
        $command > $1
    else
        $command
    fi
}
