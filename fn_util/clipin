# vim: set ft=zsh expandtab:

clipin() {
    local command=()
    case $OSTYPE in
        darwin*)
            command=(pbcopy) ;;
        *)
            command=(xclip -sel clip -i) ;;
    esac

    if (( $# > 0 )) && [[ ! -d $1 ]]; then
        $command < $1
    else
        $command
    fi
}
