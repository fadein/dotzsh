# vim: set ft=zsh expandtab:

# mount a filesystem to a directory and chdir into it
mountcd() {
	if [ -z "$1" ]; then
		print -u2 "Mount and chdir into directory DIRNAME"
		print -u2 "Usage: mountc DIRNAME"
		return 1
	fi
	mount $1 && cd $1
}
